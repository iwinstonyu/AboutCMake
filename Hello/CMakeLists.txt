CMAKE_MINIMUM_REQUIRED(VERSION 3.1)

PROJECT(Hello)

ADD_LIBRARY(Hello Hello.cpp)

AUX_SOURCE_DIRECTORY(. HELLO_FILES)
MESSAGE(STATUS "HELLO FILES: " ${HELLO_FILES})

MESSAGE(STATUS "PROJECT_SOURCE_DIR: " ${PROJECT_SOURCE_DIR})